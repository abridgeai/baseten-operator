baseten-operator {{ .Chart.AppVersion }} has been installed as release {{ .Release.Name }} in namespace {{ include "baseten-operator.namespace" . }}.

Check the controller status:
  kubectl -n {{ include "baseten-operator.namespace" . }} get deploy {{ include "baseten-operator.fullname" . }}-controller-manager

Tail the logs:
  kubectl -n {{ include "baseten-operator.namespace" . }} logs -l control-plane=controller-manager --tail=100 -f

Create a BasetenModel:
  kubectl apply -f https://raw.githubusercontent.com/abridgeai/baseten-operator/{{ .Chart.AppVersion }}/config/samples/models_v1alpha1_basetenmodel.yaml

Before the operator can reconcile, create the API key secret it reads on startup:
  kubectl -n {{ include "baseten-operator.namespace" . }} create secret generic {{ .Values.api.secretName }} \
    --from-literal={{ .Values.api.secretKey }}='<YOUR_BASETEN_API_KEY>'
