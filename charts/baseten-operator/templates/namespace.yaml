{{- if .Values.namespace.create -}}
apiVersion: v1
kind: Namespace
metadata:
  labels:
    {{- include "baseten-operator.workloadLabels" . | nindent 4 }}
  name: {{ .Values.namespace.name }}
{{- end -}}
