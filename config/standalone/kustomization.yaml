apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Standalone deployment: includes the namespace + all operator resources.
# Use this for `make deploy` or direct kubectl apply.
# GitOps users (ConfigSync, Flux, ArgoCD) should point at config/default
# and manage the namespace separately.
resources:
- ../namespace
- ../default
